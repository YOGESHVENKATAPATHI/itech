# Project identity
sonar.projectKey=YOGESHVENKATAPTHI_itech_235e972c-40db-49df-8b6d-b65639431285
sonar.projectName=itech

# Sources and tests
sonar.sources=.
sonar.tests=.

# Inclusions and test patterns
sonar.inclusions=**/*.js,**/*.jsx,**/*.ts,**/*.tsx
sonar.test.inclusions=**/*.test.js,**/*.spec.js,**/*.test.ts,**/*.spec.ts

# Coverage report
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Explicit exclusions to avoid scanning dependencies and generated files
sonar.exclusions=**/node_modules/**,**/coverage/**,**/dist/**,**/build/**,**/.git/**,**/*.min.js

# Optional: treat some folders as generated to avoid issues
sonar.generated.sources=dist,build
